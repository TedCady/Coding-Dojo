import { Router } from '@reach/router';
import { useState } from 'react';

export default () => {
    const [err, setErr] = useState();
    
    
    return(
        <Router >

        </Router>
    )
}